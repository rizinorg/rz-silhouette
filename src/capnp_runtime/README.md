Vendored Cap'n Proto C runtime sources used by `rz-silhouette`.

Source: `c-capnproto` from the pinned wrap in `subprojects/c-capnproto.wrap`.

These files are kept in-repo so downstream builds do not need the Cap'n Proto C runtime package or the `c-capnproto` Meson subproject just to build the plugin.
